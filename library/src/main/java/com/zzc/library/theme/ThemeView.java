package com.zzc.library.theme;

import android.content.Context;
import android.view.View;

/**
 * auth zzc
 * date 2018/4/8
 * desc ${desc}
 */

public class ThemeView extends View {
    public ThemeView(Context context) {
        super(context);
        throw new IllegalArgumentException("can't init,only for attr");
    }
}
