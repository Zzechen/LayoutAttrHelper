package com.zzc.library;

import android.content.Context;
import android.view.View;

/**
 * auth zzc
 * date 2018/4/4
 * desc ${desc}
 */

public class HelperView extends View {
    public HelperView(Context context) {
        super(context);
        throw new IllegalArgumentException("can't init,only for custom attrs");
    }
}
